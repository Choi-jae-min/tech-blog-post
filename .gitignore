# 이 스킬이 생성하는 블로그 글 초안 (발행 전까지는 커밋하지 않음 - 필요 없으면 이 줄 삭제)
posts/

# OS
.DS_Store
Thumbs.db

# 에디터
.vscode/
.idea/
*.swp

# 로컬 MCP 설정 (이 컴퓨터의 IDE 인스턴스에만 유효한 값이라 다른 환경에선 의미 없음)
.mcp.json

# Claude Code 로컬 권한 설정 (개인 환경 전용, 공유 설정은 .claude/settings.json에)
.claude/settings.local.json
